package protobuf;

option go_package = "protobuf/";

import "entry.proto";

message CopyState {
  required uint64 Term = 1;
  required uint64 Index = 2;
  required bool Voting = 3;
  repeated Entry Entries = 4;
}
